// Aggregates AI data to suggest practice problems
export class RecommendationEngine {
  static getDailySuggestion() {
    return { title: "Merge Intervals", reason: "You missed an interval overlap edge case yesterday." };
  }
}
